<?php
/**
 * Mageplaza
 *
 * NOTICE OF LICENSE
 *
 * This source file is subject to the mageplaza.com license that is
 * available through the world-wide-web at this URL:
 * https://www.mageplaza.com/LICENSE.txt
 *
 * DISCLAIMER
 *
 * Do not edit or add to this file if you wish to upgrade this extension to newer
 * version in the future.
 *
 * @category    Mageplaza
 * @package     Mageplaza_AutoRelated
 * @copyright   Copyright (c) 2017-2018 Mageplaza (https://www.mageplaza.com/)
 * @license     https://www.mageplaza.com/LICENSE.txt
 */

use Mageplaza\Productslider\Model\Config\Source\Additional;
?>

<?php
$helper = $this->helper('Mageplaza\Productslider\Helper\Data');
    $type = 'widget-viewed';
    $mode = 'grid';
    $productsliderId = $block->getUniqueSliderKey();
    $type = $type . '-' . $mode;

    $image = 'recently_viewed_products_grid_content_widget';
    $title = $block->getData('heading') ? $this->getData('heading') : '';
    $items = $block->getProductCollection();
    $info  = $block->getConfigValue('description');

    $description = ($mode == 'list') ? true : false;

    $title = $block->getTitle() ? $block->getTitle() : 'Mageplaza ProductSlider';
    $showPrice = $block->getShowList(Additional::SHOW_PRICE);
    $showCart = $block->getShowList(Additional::SHOW_CART);
    $showReview = $block->getShowList(Additional::SHOW_REVIEW);
?>

<?php if ($items->getSize()):?>

    <div class="mp-product-slider-block block widget block-products-list <?php echo $mode; ?>">
        <h2 class="mp-product-slider-title"><?php echo $title; ?></h2>
        <p class="mp-ps-info"><?php echo $info; ?></p>
    </div>
    <div class="block-content">
        <?php /* @escapeNotVerified */ echo '<!-- ' . $image . '-->' ?>
        <div class="products-<?php /* @escapeNotVerified */ echo $mode; ?> <?php /* @escapeNotVerified */ echo $mode; ?>">
            <ol class="product-items <?php /* @escapeNotVerified */ echo $type; ?>">
                <?php $iterator = 1; ?>
                <div class="owl-carousel owl-theme">
                    <?php foreach ($items as $_item): ?>
                        <?php /* @escapeNotVerified */ echo($iterator++ == 1) ? '<li class="product-item product-slider"">' : '</li><li class="product-item product-slider"">'  ?>
                        <div class="product-item-info">
                            <a href="<?php /* @escapeNotVerified */ echo $block->getProductUrl($_item) ?>" class="product-item-photo">
                                <?php echo $block->getImage($_item, $image)->toHtml(); ?>
                            </a>
                            <div class="product-item-details">
                                <strong class="mp-product-item-name">
                                    <a title="<?php echo $block->escapeHtml($_item->getName()) ?>"
                                       href="<?php /* @escapeNotVerified */ echo $block->getProductUrl($_item) ?>" class="product-item-link">
                                        <?php echo $block->escapeHtml($_item->getName()) ?>
                                    </a>
                                </strong>
                                <?php if ($showPrice): ?>
                                    <?php echo $block->getProductPrice($_item); ?>
                                <?php endif; ?>
                                <?php if ($showReview): ?>
                                    <?php echo $block->getReviewsSummaryHtml($_item, 'short') ?>
                                <?php endif; ?>

                                <div class="product-item-actions">
                                    <?php if ($showCart): ?>
                                        <div class="actions-primary">
                                            <?php if ($_item->isSaleable()): ?>
                                                <?php if ($_item->getTypeInstance()->hasRequiredOptions($_item)): ?>
                                                    <button class="action tocart primary"
                                                            data-mage-init='{"redirectUrl": {"url": "<?php /* @escapeNotVerified */ echo $block->getAddToCartUrl($_item) ?>"}}'
                                                            type="button" title="<?php /* @escapeNotVerified */ echo __('Add to Cart') ?>">
                                                        <span><?php /* @escapeNotVerified */ echo __('Add to Cart') ?></span>
                                                    </button>
                                                <?php else: ?>
                                                    <?php
                                                        $postDataHelper = $this->helper('Magento\Framework\Data\Helper\PostHelper');
                                                        $postData = $postDataHelper->getPostData($block->getAddToCartUrl($_item), ['product' => $_item->getEntityId()])
                                                    ?>
                                                    <button class="action tocart primary"
                                                            data-post='<?php /* @escapeNotVerified */ echo $postData; ?>'
                                                            type="button" title="<?php /* @escapeNotVerified */ echo __('Add to Cart') ?>">
                                                        <span><?php /* @escapeNotVerified */ echo __('Add to Cart') ?></span>
                                                    </button>
                                                <?php endif; ?>
                                            <?php else: ?>
                                                <?php if ($_item->getIsSalable()): ?>
                                                    <div class="stock available"><span><?php /* @escapeNotVerified */ echo __('In stock') ?></span></div>
                                                <?php else: ?>
                                                    <div class="stock unavailable"><span><?php /* @escapeNotVerified */ echo __('Out of stock') ?></span></div>
                                                <?php endif; ?>
                                            <?php endif; ?>
                                        </div>
                                    <?php endif; ?>
                                </div>
                            </div>
                        </div>
                        <?php echo($iterator == count($items)+1) ? '</li>' : '' ?>
                    <?php endforeach ?>
                </div>
            </ol>
        </div>
        <?php echo $block->getPagerHtml() ?>
    </div>
    <script>
        require([
            'jquery',
            'Mageplaza_Productslider/js/owl.carousel.min'
        ], function ($) {
            $('.owl-carousel.owl-theme').owlCarousel(<?php echo ($block->getAllOptions())?>);
            //$('#mageplaza-product-slider-<?php //echo $productsliderId;?>//').owlCarousel(<?php //echo '{is_enabled:1,loop:1,responsive:{ 0:{ items:1},600:{ items:3},1000:{ items:5}},lazyLoad:1,autoplay:1,nav:1}'?>//);
        });
    </script>
<?php endif;?>
